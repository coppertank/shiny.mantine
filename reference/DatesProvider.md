# Mantine DatesProvider (locale/date format for its children)

Mantine DatesProvider (locale/date format for its children)

## Usage

``` r
DatesProvider(...)
```

## Arguments

- ...:

  Props (`locale`, `firstDayOfWeek`, ...) and children. See
  <https://mantine.dev/dates/dates-provider/>.

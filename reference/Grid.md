# Mantine Grid family (12-column grid)

`GridCol()` must be nested inside `Grid()`.

## Usage

``` r
Grid(...)

GridCol(...)
```

## Arguments

- ...:

  Props and children (`gutter`, ...) for `Grid()`; (`span`, `offset`,
  ...) for `GridCol()`. See <https://mantine.dev/core/grid/>.

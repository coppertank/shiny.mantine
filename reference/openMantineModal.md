# Open a generic modal (`@mantine/modals`)

Open a generic modal (`@mantine/modals`)

## Usage

``` r
openMantineModal(session = shiny::getDefaultReactiveDomain(), ...)
```

## Arguments

- session:

  Session object passed to the Shiny server function.

- ...:

  Props (`title`, `children` as plain text, `size`, ...).

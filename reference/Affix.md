# Mantine Affix (fixed-position content, e.g. a "back to top" button)

Mantine Affix (fixed-position content, e.g. a "back to top" button)

## Usage

``` r
Affix(...)
```

## Arguments

- ...:

  Props (`position = list(bottom = 20, right = 20)`, ...) and children.
  See <https://mantine.dev/core/affix/>.

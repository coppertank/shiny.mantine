# Mantine JS/CSS dependency

HTML dependency that loads the compiled 'shiny.mantine' JavaScript
bundle (the 'Mantine' UI components together with its own bundled copy
of 'React' 19) and its associated styles.

## Usage

``` r
mantineDependency()
```

## Value

An `htmlDependency` object.

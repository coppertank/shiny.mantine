# Mantine Card

Mantine Card

## Usage

``` r
Card(...)
```

## Arguments

- ...:

  Props and children forwarded to Mantine's `Card` component (`shadow`,
  `padding`, `radius`, `withBorder`, ...). See
  <https://mantine.dev/core/card/>.

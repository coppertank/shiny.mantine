# Mantine Paper (surface with background/shadow/border, without Card's defaults)

Mantine Paper (surface with background/shadow/border, without Card's
defaults)

## Usage

``` r
Paper(...)
```

## Arguments

- ...:

  Props and children (`shadow`, `radius`, `withBorder`, `p`, ...). See
  <https://mantine.dev/core/paper/>.

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Pages.R
\name{Pages}
\alias{Pages}
\alias{Page}
\title{Page navigation inside an AppShell (client-side router)}
\usage{
Pages(active = NULL, ...)

Page(value, ...)
}
\arguments{
\item{active}{Initial value of the active page (must match the \code{value}
— or \code{pageValue} — of one of the \code{\link[=navLinkItem]{navLinkItem()}}s, and the \code{value} of
one of the child \code{Page()}s).}

\item{...}{Content, typically a whole \code{\link[=AppShell]{AppShell()}}.}

\item{value}{Page identifier; must match the \code{value} (or \code{pageValue})
sent by a \code{\link[=navLinkItem]{navLinkItem()}} to be selected.}
}
\description{
\code{Pages()} keeps the "active page" state entirely on the client: every
\code{\link[=navLinkItem]{navLinkItem()}} nested in the same hierarchy, when clicked, instantly
switches which \code{Page()} child is visible — without rebuilding the whole
React tree and without needing a round-trip to the server (unlike a
reactive \code{renderUI()}). The click is still sent to Shiny via the link's
\code{inputId}, so you can also react server-side if needed (analytics,
side effects, ...).
}
\details{
The whole \code{\link[=AppShell]{AppShell()}} is typically nested inside \code{Pages()}, so both the
Navbar (with the \code{navLinkItem()}s) and the Main (with the \code{Page()}s)
share the same navigation state.
}
\examples{
\dontrun{
Pages(
  active = "home",
  AppShell(
    AppShellNavbar(
      navLinkItem("navId", "home", "Home"),
      navLinkItem("navId", "settings", "Settings")
    ),
    AppShellMain(
      Page(value = "home", Text("Welcome")),
      Page(value = "settings", Text("Settings"))
    )
  )
)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Combobox.R
\name{ComboboxPopover}
\alias{ComboboxPopover}
\alias{ComboboxPopoverTarget}
\alias{updateMantineComboboxPopover}
\title{Mantine ComboboxPopover (dropdown-select attached to any custom target)}
\usage{
ComboboxPopover(inputId, ..., data = NULL, value = NULL)

ComboboxPopoverTarget(...)

updateMantineComboboxPopover(
  session = shiny::getDefaultReactiveDomain(),
  inputId,
  value = NULL,
  ...
)
}
\arguments{
\item{inputId}{Id of the Shiny input; \code{input[[inputId]]} is synced on
every selection.}

\item{...}{Other props (\code{searchable}, \code{multiple}, \code{nothingFoundMessage},
...) and a \code{ComboboxPopoverTarget()} child.}

\item{data}{Options: a character vector, or a list of \code{list(value=, label=)} (optionally grouped: \code{list(group=, items=)}). See
\url{https://mantine.dev/core/combobox-popover/}.}

\item{value}{Initial value (or \code{NULL}); a character vector if \code{multiple = TRUE}.}

\item{session}{Session object passed to the Shiny server function.}
}
\description{
Like \code{\link[=Select]{Select()}}, but renders no input of its own — supply the clickable
target (e.g. a \code{\link[=Button]{Button()}}) via \code{ComboboxPopoverTarget()}.
\code{input[[inputId]]} is synced on every selection, same as \code{Select()}.
}

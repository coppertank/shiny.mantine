% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Table.R
\name{DataTable}
\alias{DataTable}
\title{Table with search, sort, and/or selection (client-side)}
\usage{
DataTable(
  inputId,
  data,
  columns,
  selectable = FALSE,
  searchable = FALSE,
  sortable = FALSE
)
}
\arguments{
\item{inputId}{Id of the Shiny input receiving visible/selected rows.}

\item{data}{List of rows: each row is a \code{list()} with a \code{value} field
(unique id) plus one field per column.}

\item{columns}{List of \code{list(key = ..., label = ...)}, one per column
(\code{key} must match the field names in \code{data}).}

\item{selectable}{If \code{TRUE}, adds a checkbox column (+ "select all").}

\item{searchable}{If \code{TRUE}, adds a search bar that filters the rows.}

\item{sortable}{If \code{TRUE}, clicking a header sorts the table by that
column.}
}
\description{
Every time the visible or selected rows change, \code{input[[inputId]]}
receives \code{list(visible = ..., selected = ...)} (vectors of the rows'
\code{value}). For a simple table without these features, compose
\code{\link[=Table]{Table()}}/\code{\link[=TableThead]{TableThead()}}/\code{\link[=TableTr]{TableTr()}}/... by hand (see
\code{inst/examples/tables-app.R}).
}
\examples{
\dontrun{
DataTable(
  "table",
  data = list(
    list(value = 1, name = "Ada", role = "Dev"),
    list(value = 2, name = "Maria", role = "Design")
  ),
  columns = list(list(key = "name", label = "Name"), list(key = "role", label = "Role")),
  searchable = TRUE, sortable = TRUE, selectable = TRUE
)
}
}

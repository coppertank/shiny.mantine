% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OverflowList.R
\name{OverflowList}
\alias{OverflowList}
\title{Mantine OverflowList (collapses overflowing items into a "+N" badge)}
\usage{
OverflowList(
  ...,
  overflowLabel = "+{n}",
  gap = NULL,
  maxRows = NULL,
  maxVisibleItems = NULL,
  collapseFrom = NULL
)
}
\arguments{
\item{...}{Pre-built child elements, one per item (e.g. \code{Badge("Apple")},
\code{Badge("Banana")}, ...).}

\item{overflowLabel}{Template for the collapsed indicator; \code{"{n}"} is
replaced with the number of hidden items.}

\item{gap}{Key of \code{theme.spacing} or any valid CSS value for the gap
between items.}

\item{maxRows}{Number of rows to display.}

\item{maxVisibleItems}{Maximum number of visible items.}

\item{collapseFrom}{\code{"end"} (default, collapses last items) or
\code{"start"} (collapses first items — useful for breadcrumb-like
patterns).}
}
\value{
A \code{mantine_element} to nest inside \code{\link[=MantineProvider]{MantineProvider()}}. See
\url{https://mantine.dev/core/overflow-list/}.
}
\description{
Hides items that don't fit in the container and displays them as a
single collapsed indicator. Real Mantine configures this via
\code{renderItem}/\code{renderOverflow} (JS render functions, which cannot cross
the R/JSON bridge) — here, pass fully pre-built elements directly as
\code{...} (one per item, built however you like: \code{\link[=Badge]{Badge()}}s, \code{\link[=Pill]{Pill()}}s,
\code{\link[=Avatar]{Avatar()}}s, ...); the ones that don't fit are collapsed into a single
\code{\link[=Badge]{Badge()}} using \code{overflowLabel} as a template (\code{"{n}"} is replaced with
the hidden count).
}
\examples{
\dontrun{
OverflowList(
  Badge("Apple"), Badge("Banana"), Badge("Cherry"), Badge("Date"),
  overflowLabel = "+{n} more"
)
}
}

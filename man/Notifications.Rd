% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Notifications.R
\name{Notifications}
\alias{Notifications}
\title{Mantine-styled notifications}
\usage{
Notifications(...)
}
\arguments{
\item{...}{Props for the container (\code{position}, \code{zIndex}, \code{limit}, ...).
See \url{https://mantine.dev/x/notifications/}.}
}
\description{
Unlike \code{shiny::showNotification()} (Bootstrap-styled, clashing with the
Mantine theme), these notifications use \verb{@mantine/notifications} — the
same library as the components, the same theme/color-scheme.
}
\details{
\code{Notifications()} needs to be mounted \strong{once} in the page (typically
inside the main \code{MantineProvider()}); after that,
\code{showMantineNotification()} can be called from any server-side
\code{observeEvent()}/reactive.
}
\examples{
\dontrun{
# ui:
MantineProvider(Notifications(position = "top-right"), ...)

# server:
observeEvent(input$save_btn, {
  showMantineNotification(session, title = "Saved", message = "Changes saved successfully", color = "green")
})
}
}

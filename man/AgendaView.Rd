% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Schedule.R
\name{AgendaView}
\alias{AgendaView}
\title{Mantine AgendaView (\verb{@mantine/schedule}): events as a vertical list}
\usage{
AgendaView(inputId, rangeStart, rangeEnd, events = list(), ...)
}
\arguments{
\item{inputId}{Id used as the prefix for this view's Shiny inputs.}

\item{rangeStart, rangeEnd}{Start/end of the displayed date range (each a
\code{"YYYY-MM-DD"} string, or an R \code{Date}/\code{POSIXct}).}

\item{events}{Events to display; see \code{\link[=DayView]{DayView()}}'s \code{events}.}

\item{...}{Other props (\code{headerFormat}, \code{dateHeaderFormat}, \code{locale},
\code{labels}, ...). See \url{https://mantine.dev/schedule/agenda-view/}.}
}
\value{
A \code{mantine_element} to nest inside \code{\link[=MantineProvider]{MantineProvider()}}.
}
\description{
Displays events in a scrollable list grouped by date, in chronological
order, over a fixed \verb{[rangeStart, rangeEnd]} window — no drag-and-drop,
no \code{date}/\code{onDateChange} navigation of its own (the app controls the
visible range directly). \code{input[[paste0(inputId, "_event_click")]]}
receives the clicked event's \code{id}.
}
\examples{
\dontrun{
AgendaView(
  inputId = "agenda1",
  rangeStart = Sys.Date(), rangeEnd = Sys.Date() + 7,
  events = data.frame(
    id = 1, title = "Team standup",
    start = "2026-07-27 09:00:00", end = "2026-07-27 09:30:00"
  )
)
}
}

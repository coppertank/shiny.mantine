% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mantine-element.R
\name{ensureArray}
\alias{ensureArray}
\title{Coerce a value to always serialize as a JSON array}
\usage{
ensureArray(x)
}
\description{
For props the JS side always expects as an array (e.g. \code{MultiSelect()}'s
\code{value}, a vector of selected values) — never \code{NULL}/\code{null} (the JS side
typically calls \code{.map()} on it directly) and never a bare scalar. Two
independent \code{jsonlite::toJSON(..., auto_unbox = TRUE)} calls in this
package (\code{renderMantineRoot()} for the initial element tree,
\code{session$sendCustomMessage()}'s own default serializer for
\code{updateMantineXxx()} calls) both auto-unbox a length-1 atomic vector to a
bare JSON value (\code{"x"} instead of \verb{["x"]}) and serialize \code{NULL} as
\code{null} — either would crash a component whose JS side unconditionally
\code{.map()}s over the value. \code{as.list()} sidesteps both: it turns any
length-1 (or longer) atomic vector into a genuine (unnamed) R list,
which this package's serialization always treats as a JSON array
regardless of length, and \code{as.list(NULL)} is already \code{list()} (an empty
array), not \code{NULL}.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AdvancedComponents.R
\name{Transition}
\alias{Transition}
\title{Mantine Transition (mount/unmount animation)}
\usage{
Transition(mounted = TRUE, ...)
}
\arguments{
\item{mounted}{Whether the children should be visible/mounted.}

\item{...}{Props (\code{transition}, \code{duration}, \code{timingFunction}, ...) and the
children to animate. See \url{https://mantine.dev/core/transition/}.}
}
\description{
Animates its children in and out based on \code{mounted}. Mantine's own
\code{Transition} takes a render-prop \code{children} (a function receiving the
current transition styles) — incompatible with R's plain,
data-only \code{...} children, so this wraps it internally and applies the
styles to a plain wrapper \verb{<div>} around your (static) children instead.
}
\examples{
\dontrun{
Transition(
  mounted = input$show, transition = "fade", duration = 200,
  Card(withBorder = TRUE, Text("Animated in/out"))
)
}
}

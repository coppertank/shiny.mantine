% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Modals.R
\name{ModalsProvider}
\alias{ModalsProvider}
\title{Imperative modals (\verb{@mantine/modals})}
\usage{
ModalsProvider(...)
}
\arguments{
\item{...}{Children and props (\code{ModalsProvider()}); see
\url{https://mantine.dev/x/modals/}.}
}
\description{
Imperative API to open modals (confirmation or generic) from a single
server-side call, without having to declare a \code{\link[=Modal]{Modal()}} in the UI ahead
of time. Only supports simple props (no nested Mantine component trees:
for a modal with rich Mantine content, compose \code{\link[=Modal]{Modal()}} with
\code{\link[=updateMantineProps]{updateMantineProps()}} instead).
}
\details{
Requires \code{ModalsProvider()} mounted \strong{once} in the page, wrapping the
content (like \code{MantineProvider()}).
}
\examples{
\dontrun{
# ui:
ModalsProvider(MantineProvider(...))

# server:
observeEvent(input$delete_btn, {
  openMantineConfirmModal(
    session, inputId = "confirm_delete",
    title = "Confirm deletion",
    message = "This action cannot be undone. Continue?",
    labels = list(confirm = "Delete", cancel = "Cancel"),
    confirmProps = list(color = "red")
  )
})
observeEvent(input$confirm_delete, {
  if (isTRUE(input$confirm_delete)) {
    # ... actually delete ...
  }
})
}
}

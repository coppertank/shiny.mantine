% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Charts.R
\name{ScatterChart}
\alias{ScatterChart}
\title{Mantine ScatterChart (x/y scatter plot)}
\usage{
ScatterChart(data, dataKey, ...)
}
\arguments{
\item{data}{List of \code{list(name = ..., color = ..., data = list(list(x = ..., y = ...), ...))}, one entry per series.}

\item{dataKey}{\code{list(x = "col", y = "col")} — the field names read from
each point.}

\item{...}{Other props. See \url{https://mantine.dev/charts/scatter-chart/}.}
}
\description{
Unlike the other charts, \code{data} is a list of \emph{series}, each with its own
nested points — pass it exactly in Mantine's shape (no automatic
\code{data.frame} conversion, since there is no single flat table to convert).
}
\examples{
\dontrun{
ScatterChart(
  data = list(list(
    name = "Group A", color = "blue.5",
    data = list(list(age = 20, bmi = 22), list(age = 30, bmi = 25))
  )),
  dataKey = list(x = "age", y = "bmi")
)
}
}

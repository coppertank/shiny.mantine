% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Schedule.R
\name{ResourcesDayView}
\alias{ResourcesDayView}
\alias{ResourcesWeekView}
\alias{ResourcesMonthView}
\title{Mantine resource schedule views (\verb{@mantine/schedule}): resources as rows/columns}
\usage{
ResourcesDayView(
  inputId,
  date = NULL,
  resources = list(),
  events = list(),
  ...
)

ResourcesWeekView(
  inputId,
  date = NULL,
  resources = list(),
  events = list(),
  ...
)

ResourcesMonthView(
  inputId,
  date = NULL,
  resources = list(),
  events = list(),
  ...
)
}
\arguments{
\item{inputId}{Id of the Shiny input holding the currently displayed
\code{date}.}

\item{date}{Initial displayed date (a \code{"YYYY-MM-DD"} string, or an R
\code{Date}/\code{POSIXct}).}

\item{resources}{Resources to group by: a \code{data.frame} (or list of
rows) with \code{id} and \code{label} columns.}

\item{events}{Events to display; see \code{\link[=DayView]{DayView()}}'s \code{events} — each
event's \code{resourceId} should match one of \code{resources}' \code{id}.}

\item{...}{Other props (\code{intervalMinutes}, \code{startTime}, \code{endTime},
\code{withEventsDragAndDrop}, \code{withEventResize}, \code{withDragSlotSelect},
\code{groups}, \code{groupLabelWidth}, \code{maxEventsPerTimeSlot}, ...). See
\url{https://mantine.dev/schedule/resources-day-view/} /
\url{https://mantine.dev/schedule/resources-week-view/} /
\url{https://mantine.dev/schedule/resources-month-view/}.}
}
\value{
A \code{mantine_element} to nest inside \code{\link[=MantineProvider]{MantineProvider()}}.
}
\description{
Like \code{\link[=DayView]{DayView()}}/\code{\link[=WeekView]{WeekView()}}/\code{\link[=MonthView]{MonthView()}}, but events are grouped by
\code{resources} (e.g. meeting rooms, staff members) instead of a single
timeline — each event's \code{resourceId} matches a resource's \code{id}. Reports
interactions to Shiny the same way as \code{\link[=DayView]{DayView()}} (see \code{?ScheduleViews}
for the full list of suffixed input ids); \verb{_event_drop}/\verb{_event_resize}
payloads additionally include \code{resourceId} when the event was moved to
a different resource.
}
\details{
\code{intervalMinutes} (default \code{60}) accepts values that divide evenly into
an hour (e.g. \code{15}, \code{30}) \emph{or} whole numbers of hours (e.g. \code{120},
\code{240}): values above \code{60} widen each time-slot column to span several
hours instead of subdividing within an hour — useful to fit a full day
in view compactly (e.g. \code{intervalMinutes = 240} renders 4-hour-wide
columns spanning midnight to midnight).
}
\examples{
\dontrun{
ResourcesDayView(
  inputId = "rooms1",
  date = Sys.Date(),
  resources = data.frame(
    id = c("tokyo", "paris"),
    label = c("Meeting room: Tokyo", "Meeting room: Paris")
  ),
  events = data.frame(
    id = 1, title = "Team standup",
    start = "2026-07-27 09:00:00", end = "2026-07-27 10:00:00",
    resourceId = "tokyo"
  ),
  intervalMinutes = 240
)
}
}

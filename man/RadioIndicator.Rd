% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AdvancedComponents.R
\name{RadioIndicator}
\alias{RadioIndicator}
\alias{CheckboxIndicator}
\title{Mantine RadioIndicator / CheckboxIndicator (standalone, decorative)}
\usage{
RadioIndicator(checked = FALSE, ...)

CheckboxIndicator(checked = FALSE, indeterminate = FALSE, ...)
}
\arguments{
\item{checked}{Whether to show the checked state.}

\item{...}{Other props (\code{color}, \code{size}, \code{radius}, ...). See
\url{https://mantine.dev/core/radio/#radioindicator}.}

\item{indeterminate}{Shows the indeterminate state (ignores \code{checked}).}
}
\description{
The visual "checked" indicator \code{\link[=Radio]{Radio()}}/\code{\link[=Checkbox]{Checkbox()}} render
internally, usable on its own wherever you need the same look without a
real input behind it — e.g. a read-only status list, or a custom row
that toggles some other way. Purely decorative: \code{checked}/
\code{indeterminate} are plain display props, not synced with Shiny (pair
with \code{\link[=updateMantineProps]{updateMantineProps()}} if you need to change them from the
server).
}

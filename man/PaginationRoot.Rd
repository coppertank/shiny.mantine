% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpecialInputs.R
\name{PaginationRoot}
\alias{PaginationRoot}
\title{Mantine Pagination.Root (fully custom pagination layout)}
\usage{
PaginationRoot(inputId, total, ..., value = 1)
}
\arguments{
\item{inputId}{Id of the Shiny input; \code{input[[inputId]]} is synced on
page change.}

\item{total}{Total number of pages.}

\item{...}{Children (typically \code{\link[=PaginationFirst]{PaginationFirst()}},
\code{\link[=PaginationPrevious]{PaginationPrevious()}}, \code{\link[=PaginationItems]{PaginationItems()}}, \code{\link[=PaginationNext]{PaginationNext()}},
\code{\link[=PaginationLast]{PaginationLast()}}) and other props (\code{siblings}, \code{boundaries}, ...).
See \url{https://mantine.dev/core/pagination/#compound-components}.}

\item{value}{Initial page. Must be passed by name (as in the example
below): with children in \code{...}, an unnamed argument here would bind
to \code{value} positionally instead of joining the children, since \code{...}
only captures unnamed arguments that come \emph{after} it.}
}
\description{
The lower-level piece behind \code{\link[=Pagination]{Pagination()}}, for a fully custom
arrangement of the prev/next/first/last controls (reordering them,
dropping some, adding your own elements in between) — compose it with
\code{\link[=PaginationFirst]{PaginationFirst()}}, \code{\link[=PaginationPrevious]{PaginationPrevious()}}, \code{\link[=PaginationItems]{PaginationItems()}},
\code{\link[=PaginationNext]{PaginationNext()}}, \code{\link[=PaginationLast]{PaginationLast()}} and \code{\link[=PaginationDots]{PaginationDots()}}, which
read the current page from it automatically (no per-part wiring
needed). Stateful exactly like \code{\link[=Pagination]{Pagination()}}:
\code{input[[inputId]]} is synced on page change, and
\code{\link[=updateMantinePagination]{updateMantinePagination()}} works on it the same way.
}
\examples{
\dontrun{
Group(
  PaginationRoot(
    inputId = "page", total = 10,
    PaginationFirst(), PaginationPrevious(), PaginationItems(),
    PaginationNext(), PaginationLast()
  )
)
}
}

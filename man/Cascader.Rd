% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AdvancedComponents.R
\name{Cascader}
\alias{Cascader}
\alias{updateMantineCascader}
\title{Mantine Cascader (Shiny stateful input, hierarchical cascading selection)}
\usage{
Cascader(inputId, data, value = NULL, ...)

updateMantineCascader(
  session = shiny::getDefaultReactiveDomain(),
  inputId,
  value = NULL,
  ...
)
}
\arguments{
\item{inputId}{Id of the Shiny input; \code{input[[inputId]]} (a character
vector, the path from root to the selected node) is synced on every
selection.}

\item{data}{Hierarchical options: a list of \code{list(value=, label=, children=, disabled=)}, \code{children} nested the same way. Every \code{value}
must be unique across the whole tree.}

\item{value}{Initial value: a character vector (the path from root to
the selected node), or \code{NULL} for nothing selected. Only leaf nodes
are selectable unless \code{changeOnSelect = TRUE}.}

\item{...}{Other props (\code{changeOnSelect}, \code{expandTrigger} =
\code{"click"}/\code{"hover"}, \code{searchable}, \code{withColumns}, \code{clearable},
\code{maxDisplayedLevels}, ...). See \url{https://mantine.dev/core/cascader/}.}

\item{session}{Session object passed to the Shiny server function.}
}
\description{
Lets users drill down through nested options column by column (or, with
\code{withColumns = FALSE}, as a flat searchable list of full paths) — e.g.
picking a location through Continent > Country > City. Unlike
\code{\link[=TreeSelect]{TreeSelect()}} (a flat popover tree), each level opens its own column
next to the previous one.
}
\examples{
\dontrun{
Cascader(
  inputId = "location",
  label = "Location",
  data = list(
    list(value = "asia", label = "Asia", children = list(
      list(value = "jp", label = "Japan", children = list(
        list(value = "tokyo", label = "Tokyo"),
        list(value = "osaka", label = "Osaka")
      ))
    ))
  )
)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Combobox.R
\name{Combobox}
\alias{Combobox}
\alias{ComboboxTarget}
\alias{ComboboxEventsTarget}
\alias{ComboboxDropdownTarget}
\alias{ComboboxDropdown}
\alias{ComboboxOptions}
\alias{ComboboxOption}
\alias{ComboboxSearch}
\alias{updateMantineComboboxSearch}
\alias{ComboboxEmpty}
\alias{ComboboxFooter}
\alias{ComboboxHeader}
\alias{ComboboxGroup}
\alias{ComboboxChevron}
\alias{ComboboxClearButton}
\alias{ComboboxHiddenInput}
\title{Mantine Combobox family (headless dropdown primitive for custom selects)}
\usage{
Combobox(..., inputId = NULL, opened = FALSE)

ComboboxTarget(...)

ComboboxEventsTarget(...)

ComboboxDropdownTarget(...)

ComboboxDropdown(...)

ComboboxOptions(...)

ComboboxOption(value, ...)

ComboboxSearch(inputId, value = "", ...)

updateMantineComboboxSearch(
  session = shiny::getDefaultReactiveDomain(),
  inputId,
  value = NULL,
  ...
)

ComboboxEmpty(...)

ComboboxFooter(...)

ComboboxHeader(...)

ComboboxGroup(...)

ComboboxChevron(...)

ComboboxClearButton(...)

ComboboxHiddenInput(...)
}
\arguments{
\item{...}{Other props and children — typically a \code{\link[=ComboboxTarget]{ComboboxTarget()}}
and a \code{\link[=ComboboxDropdown]{ComboboxDropdown()}} (\code{width}, \code{position}, \code{shadow}, \code{withArrow},
...).}

\item{inputId}{Id of the Shiny input that receives the search text on
every keystroke (like \code{\link[=TextInput]{TextInput()}}).}

\item{opened}{Whether the dropdown is open. Toggle from the server with
\code{\link[=updateMantineProps]{updateMantineProps()}} (needs a \code{mantineId}).}

\item{value}{Initial search text.}

\item{session}{Session object passed to the Shiny server function.}
}
\description{
\code{Combobox()} is Mantine's low-level building block for custom
select/autocomplete/multiselect UIs — the same primitive
\code{\link[=Select]{Select()}}/\code{\link[=MultiSelect]{MultiSelect()}}/\code{\link[=Autocomplete]{Autocomplete()}}/\code{\link[=TagsInput]{TagsInput()}}/\code{\link[=PillsInput]{PillsInput()}}
are built on top of internally. Reach for it only when those don't fit
(e.g. options need custom icons/layout beyond \code{renderOption}-style
composition, or a non-input target like a \code{Button()}). See
\url{https://mantine.dev/core/combobox/}.
}
\details{
The real Mantine \code{Combobox} is driven by an imperative \code{useCombobox()}
store (a live JS object with methods); this package creates and owns
that store for you internally, so the pieces below stay fully
declarative:
\itemize{
\item \code{ComboboxTarget()}'s single child (e.g. a \code{\link[=Button]{Button()}} or \code{\link[=TextInput]{TextInput()}})
automatically toggles the dropdown on click — real Mantine requires
wiring this by hand (\verb{onClick=\{() => combobox.toggleDropdown()\}}),
not needed here.
\item \code{ComboboxSearch()} is a text field wired to Shiny like any other input
(\code{input[[inputId]]} on every keystroke) and opens the dropdown on
focus — pair it with \code{\link[=renderMantine]{renderMantine()}}/\code{\link[=mantineOutput]{mantineOutput()}} to re-render
\code{ComboboxOptions()} server-side as the user types (server-side
filtering), since an R render function cannot cross the bridge the
way Mantine's own \code{filter}/\code{renderOption} props do.
\item Selecting a \code{ComboboxOption()} (click or Enter) sends its \code{value} to
\code{input[[inputId]]} of the enclosing \code{Combobox()} (as an event, like
\code{\link[shiny:actionButton]{shiny::actionButton()}}) and closes the dropdown. Highlighting the
current selection (\code{active = TRUE} on the matching \code{ComboboxOption()})
is the caller's responsibility, same as Mantine's own controlled
examples.
}
}

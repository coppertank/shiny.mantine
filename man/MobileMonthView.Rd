% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Schedule.R
\name{MobileMonthView}
\alias{MobileMonthView}
\title{Mantine MobileMonthView (\verb{@mantine/schedule}): touch-optimized month view}
\usage{
MobileMonthView(
  inputId,
  date = NULL,
  selectedDate = NULL,
  events = list(),
  ...
)
}
\arguments{
\item{inputId}{Id of the Shiny input holding the selected day.}

\item{date}{Initial displayed month (a \code{"YYYY-MM-DD"} string, or an R
\code{Date}/\code{POSIXct}).}

\item{selectedDate}{Initial selected day, or \code{NULL}.}

\item{events}{Events to display; see \code{\link[=DayView]{DayView()}}'s \code{events}.}

\item{...}{Other props (\code{withWeekNumbers}, \code{firstDayOfWeek},
\code{highlightToday}, ...). See
\url{https://mantine.dev/schedule/mobile-month-view/}.}
}
\value{
A \code{mantine_element} to nest inside \code{\link[=MantineProvider]{MantineProvider()}}.
}
\description{
A month grid with event indicators on top and a list of the selected
day's events at the bottom — designed for touch interaction, like a
mobile calendar app; no drag-and-drop. \code{input[[inputId]]} holds the
selected day (a \code{"YYYY-MM-DD"} string, the primary value, like every
other stateful input in this package); \code{input[[paste0(inputId, "_month")]]} holds the displayed month (secondary, changes on
navigation alone). Update either from the server with
\code{\link[=updateMantineProps]{updateMantineProps()}} (needs a \code{mantineId}; patch \code{selectedDate}
and/or \code{date}).
}
\examples{
\dontrun{
MobileMonthView(inputId = "mob1", date = Sys.Date(), selectedDate = Sys.Date())
}
}

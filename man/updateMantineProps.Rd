% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ReactiveProps.R
\name{updateMantineProps}
\alias{updateMantineProps}
\title{Update arbitrary props of a mounted component (generic channel)}
\usage{
updateMantineProps(session = shiny::getDefaultReactiveDomain(), mantineId, ...)
}
\arguments{
\item{session}{Session object passed to the Shiny server function.}

\item{mantineId}{The value passed as \code{mantineId = "..."} to the
component you want to update.}

\item{...}{The props to update (valid names for the target Mantine
component), as named arguments.}
}
\value{
None. Called for its side effect.
}
\description{
Unlike \code{updateMantineTextInput()} and friends (which only update an
input's \code{value}), this function updates \emph{any} simple prop (strings,
numbers, booleans, lists — not nested component trees) of \emph{any}
component that has a matching \code{mantineId} prop. This is the mechanism
that makes it possible, for example, to open and close
\code{Modal()}/\code{Drawer()}/\code{Dialog()} from R (the \code{opened} prop), or to
dynamically change a component's \code{disabled}/\code{color}/\code{label} without
rebuilding the whole tree.
}
\details{
Not every component supports \code{mantineId}: it is supported by overlays
(\code{Modal}, \code{Drawer}, \code{Dialog}, \code{Popover}, \code{Affix}, \code{LoadingOverlay}),
feedback elements (\code{Alert}, \code{Badge}, \code{Indicator}) and every stateful
input (\code{TextInput}, \code{Select}, \code{Switch}, \code{Checkbox}, \code{Autocomplete},
\code{NumberInput}, \code{PasswordInput}, \code{SegmentedControl}, \code{Slider},
\code{RangeSlider}) — for the latter this complements (does not replace)
\code{updateMantineTextInput()} and friends, which remain the dedicated way
to update \code{value}.

To update \emph{nested content} (e.g. a \code{Modal}'s children) rather than
simple props, use \code{\link[=mantineOutput]{mantineOutput()}}/\code{\link[=renderMantine]{renderMantine()}}, which
reactively recompute an entire sub-tree.
}
\examples{
\dontrun{
# server:
observeEvent(input$open_modal_btn, {
  updateMantineProps(session, "my_modal", opened = TRUE)
})
observeEvent(input$close_modal_btn, {
  updateMantineProps(session, "my_modal", opened = FALSE)
})
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MantineOutput.R
\name{mantineOutput}
\alias{mantineOutput}
\alias{renderMantine}
\title{Reactive Mantine output}
\usage{
mantineOutput(outputId)

renderMantine(expr, env = parent.frame(), quoted = FALSE)
}
\arguments{
\item{outputId}{Id of the Shiny output.}

\item{expr}{Expression that returns a \code{shiny.mantine} component (or a
value/tag to render as a child).}

\item{env}{Environment in which to evaluate \code{expr}.}

\item{quoted}{Is \code{expr} already quoted?}
}
\value{
A \code{shiny.tag} to insert into the UI.
}
\description{
Analogous to \code{shiny::uiOutput()} but for a Mantine sub-tree: the content
is reactively recomputed server-side with \code{\link[=renderMantine]{renderMantine()}}, without
needing to rebuild the whole page (unlike \code{MantineProvider()}, which
mounts statically once at page load). Fills the architectural gap of not
having a \code{renderUI()}/\code{reactOutput()} equivalent for Mantine.
}
\details{
Each \code{mantineOutput()} mounts its own independent React root (like every
\code{MantineProvider()}): if you want Mantine's theme/color-scheme inside the
output, wrap the content in a \code{MantineProvider()} inside the expression
passed to \code{\link[=renderMantine]{renderMantine()}}. Every time the expression is re-evaluated,
the whole sub-tree is re-rendered: local component state inside it (e.g.
text typed into a \code{TextInput} not controlled by a stable \code{inputId}) can
be lost, exactly as with \code{renderUI()}.
}
\examples{
\dontrun{
# ui:
mantineOutput("my_output")

# server:
output$my_output <- renderMantine({
  MantineProvider(
    Card(withBorder = TRUE, Text(paste("Updated at", Sys.time())))
  )
})
}
}

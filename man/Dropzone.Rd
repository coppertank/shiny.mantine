% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Dropzone.R
\name{Dropzone}
\alias{Dropzone}
\alias{DropzoneAccept}
\alias{DropzoneReject}
\alias{DropzoneIdle}
\title{Mantine Dropzone (drag & drop upload)}
\usage{
Dropzone(inputId, ..., accept = NULL, maxSize = NULL)

DropzoneAccept(...)

DropzoneReject(...)

DropzoneIdle(...)
}
\arguments{
\item{inputId}{Id of the Shiny input receiving file metadata.}

\item{...}{Children (typically \code{\link[=DropzoneAccept]{DropzoneAccept()}}/\code{\link[=DropzoneReject]{DropzoneReject()}}/
\code{\link[=DropzoneIdle]{DropzoneIdle()}} with icons + text) and other props forwarded to
\code{Dropzone} (\code{multiple}, \code{loading}, ...). See
\url{https://mantine.dev/x/dropzone/}.}

\item{accept}{Vector of accepted MIME types (e.g.
\code{c("application/pdf")}); \code{NULL} to accept everything.}

\item{maxSize}{Maximum size per file in bytes (\code{NULL} for no limit).}
}
\description{
When files are dropped (or selected via click), \code{input[[inputId]]}
receives \verb{list(count = <number of files>, files = <one element per file, with name/size/type>)} — the file \emph{content} is not uploaded over
this channel. If you need to actually upload the files, pair this with
a regular \code{shiny::fileInput()} (you can hide it via CSS and trigger it
from a click on the Dropzone). Always use \code{input[[inputId]]$count} for
the file count (not \code{length(input[[inputId]])}): a JSON array with a
single object gets "flattened" by R differently than one with several
objects, making \code{length()} on \verb{$files} alone unreliable.
}

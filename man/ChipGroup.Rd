% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpecialInputs.R
\name{ChipGroup}
\alias{ChipGroup}
\alias{updateMantineChipGroup}
\alias{Chip}
\title{Mantine Chip / ChipGroup (Shiny stateful input)}
\usage{
ChipGroup(inputId, ..., value = NULL, multiple = FALSE)

updateMantineChipGroup(
  session = shiny::getDefaultReactiveDomain(),
  inputId,
  value = NULL,
  ...
)

Chip(value, label = NULL, ...)
}
\arguments{
\item{inputId}{Id of the Shiny input; \code{input[[inputId]]} is synced to
the selection (a string, or a vector if \code{multiple = TRUE}).}

\item{...}{Other props.}

\item{value}{Initial value of the group (for \code{ChipGroup()}) or the
option's identifier (for \code{Chip()}).}

\item{multiple}{If \code{TRUE}, more than one \code{Chip()} can be selected at once
and \code{input[[inputId]]} becomes a character vector.}

\item{session}{Session object passed to the Shiny server function.}

\item{label}{Text of the individual Chip.}
}
\description{
\code{Chip()} must be nested inside \code{ChipGroup()} — Mantine automatically
links each child \code{Chip()} to the group's state (single or multiple
selection depending on \code{multiple}).
}

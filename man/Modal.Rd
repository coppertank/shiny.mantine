% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Overlays.R
\name{Modal}
\alias{Modal}
\title{Mantine Modal}
\usage{
Modal(mantineId, ..., inputId = NULL, opened = FALSE)
}
\arguments{
\item{mantineId}{Identifier for \code{\link[=updateMantineProps]{updateMantineProps()}} (opening/closing
from R). Required: without it, the Modal cannot be controlled from R
(it would stay in its initial \code{opened} state).}

\item{...}{Children (the modal's content) and other props (\code{title},
\code{size}, \code{centered}, ...). See \url{https://mantine.dev/core/modal/}.}

\item{inputId}{If provided, receives \code{FALSE} when the user closes the
modal.}

\item{opened}{Initial state (usually \code{FALSE}: opened from R at the
right moment with \code{updateMantineProps()}).}
}
\description{
Open/close from R with
\code{updateMantineProps(session, mantineId, opened = TRUE/FALSE)}. When the
user closes it (X, click outside, Escape), \code{input[[inputId]]} (if
provided) receives \code{FALSE}.
}
\examples{
\dontrun{
# ui:
Modal("confirm_modal", inputId = "confirm_modal_state", title = "Confirm",
  Text("Are you sure?"),
  Button("Confirm", inputId = "confirm_btn")
)
Button("Open modal", inputId = "open_modal_btn")

# server:
observeEvent(input$open_modal_btn, {
  updateMantineProps(session, "confirm_modal", opened = TRUE)
})
observeEvent(input$confirm_btn, {
  updateMantineProps(session, "confirm_modal", opened = FALSE)
})
}
}

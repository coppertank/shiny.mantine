% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components.R
\name{Tabs}
\alias{Tabs}
\alias{TabsList}
\alias{TabsTab}
\alias{TabsPanel}
\title{Mantine Tabs family}
\usage{
Tabs(..., inputId = NULL)

TabsList(...)

TabsTab(value, ...)

TabsPanel(value, ...)
}
\arguments{
\item{...}{Props and children for \code{Tabs()} (typically a \code{\link[=TabsList]{TabsList()}}
containing \code{TabsTab()}s); for \code{TabsTab()}/\code{TabsPanel()}, other props
(\code{leftSection}, ...) and the content (label or panel) as an unnamed
child. See \url{https://mantine.dev/core/tabs/}.}

\item{inputId}{If provided, every tab change also sends
\code{input[[inputId]]} (the selected tab's \code{value}) — you can reuse the
same \code{inputId} as \code{\link[=navLinkItem]{navLinkItem()}} to have them feed into the same
Shiny input.}

\item{value}{Tab identifier (must match the \code{value} of a \code{\link[=Page]{Page()}} to
participate in page navigation).}
}
\description{
\code{Tabs()} (the container) is "aware" of the \code{\link[=Pages]{Pages()}} router: if nested
inside it, selecting a tab changes which \code{\link[=Page]{Page()}} is visible — exactly
like \code{\link[=navLinkItem]{navLinkItem()}} — useful to offer the same navigation both in the
Navbar (with \code{NavLink}) and in a header (with \code{Tabs}), staying in sync.
Each \code{TabsTab()}'s \code{value} must match the \code{value} of a \code{Page()}. When
used outside \code{Pages()}, it behaves like a normal controlled \code{Tabs}
(local state).
}

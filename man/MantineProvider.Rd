% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MantineProvider.R
\name{MantineProvider}
\alias{MantineProvider}
\title{MantineProvider}
\usage{
MantineProvider(
  ...,
  defaultColorScheme = c("light", "dark", "auto"),
  theme = NULL,
  containerId = NULL,
  fixShinyFontScale = TRUE
)
}
\arguments{
\item{...}{'shiny.mantine' components (e.g. \code{\link[=Button]{Button()}}, \code{\link[=TextInput]{TextInput()}})
and/or plain 'htmltools' tags to render as children. Plain 'htmltools'
tags (e.g. \code{tags$h2()}) are rendered as static markup. Do not nest
\code{shiny::textOutput()} or other native Shiny bindings here: the content
is inserted via \code{dangerouslySetInnerHTML} and Shiny does not
automatically re-scan it for bindings. Put native Shiny outputs/inputs
outside \code{MantineProvider()}.}

\item{defaultColorScheme}{One of \code{"light"}, \code{"dark"}, \code{"auto"}.}

\item{theme}{A list overriding Mantine's default theme (colors, fonts,
spacing, ...). See \url{https://mantine.dev/theming/theme-object/}. If
\code{theme$scale} is not set and \code{fixShinyFontScale} is \code{TRUE} (the
default), it is computed automatically at mount time — see
\code{fixShinyFontScale}.}

\item{containerId}{Fixed HTML id for the mount point. Generated
automatically if omitted.}

\item{fixShinyFontScale}{Shiny's bundled Bootstrap 3 CSS (attached by
\code{fluidPage()}/\code{bootstrapPage()}, but \emph{not} by a plain \code{tagList()} UI)
sets \verb{html \{ font-size: 10px; \}}, while Mantine's entire size scale
(font sizes, spacing, component dimensions) is expressed in \code{rem}
units calibrated for the browser default of a 16px root — so under a
10px root every Mantine component renders at 62.5\% of its intended
size, and conversely a blindly-applied fix would \emph{over}-correct on
pages whose root really is 16px (e.g. \code{tagList()}-based UIs, which
don't load Bootstrap at all). Since the actual root font-size depends
entirely on which page function and CSS the surrounding app happens to
load — something R has no way to know ahead of time — this can't be
fixed with a fixed guess baked in on the R side. Instead, when \code{TRUE}
(the default), the JS side measures the real, already-computed root
font-size once at mount time and sets \verb{theme$scale <- 16 / <measured px>} via Mantine's own built-in \code{--mantine-scale} mechanism — correct
whether the actual root is 10px, 16px, or anything else (e.g. a
browser accessibility "larger text" setting). Set to \code{FALSE} to get
Mantine's untouched default sizing, or supply your own \code{theme$scale}
to take over the calculation yourself (either disables auto-detection).}
}
\value{
A \code{shiny.tag.list} to insert into a Shiny app's UI.
}
\description{
Provides 'Mantine's theme/spacing/color context, including the
light/dark color scheme. Every 'shiny.mantine' component must sit,
directly or indirectly, inside a single \code{MantineProvider()} call:
Mantine components used outside a provider will not render correctly
(missing theme/CSS variables).
}

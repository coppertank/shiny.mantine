% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AdvancedComponents.R
\name{Notification}
\alias{Notification}
\title{Mantine Notification (static notification box)}
\usage{
Notification(...)
}
\arguments{
\item{...}{Props and children (\code{title}, \code{color}, \code{icon}, \code{withCloseButton},
...). See \url{https://mantine.dev/core/notification/}.}
}
\description{
A notification-styled box rendered directly in the page layout — distinct
from the toast popups shown by \code{\link[=showMantineNotification]{showMantineNotification()}} (the
\verb{@mantine/notifications} system), which mount/unmount on their own outside
the normal component tree. Use this one when you want a dismissable
notification-styled box as part of your regular layout (e.g. inside
\code{\link[=renderMantine]{renderMantine()}}/\code{\link[=mantineOutput]{mantineOutput()}} to show/hide it reactively) instead of
a transient popup. \code{onClose} is not wired to Shiny by default; if you need
the built-in close button to do something, pair this with
\code{mantineOutput()}/\code{renderMantine()} and drive visibility from the server.
}

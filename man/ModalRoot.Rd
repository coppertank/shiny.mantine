% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Overlays.R
\name{ModalRoot}
\alias{ModalRoot}
\title{Mantine Modal.Root (fully custom modal layout)}
\usage{
ModalRoot(mantineId, ..., inputId = NULL, opened = FALSE)
}
\arguments{
\item{mantineId}{Identifier for \code{\link[=updateMantineProps]{updateMantineProps()}}.}

\item{...}{Children — typically \code{\link[=ModalOverlay]{ModalOverlay()}} and \code{\link[=ModalContent]{ModalContent()}}
(wrapping \code{\link[=ModalHeader]{ModalHeader()}} with \code{\link[=ModalTitle]{ModalTitle()}} +
\code{\link[=ModalCloseButton]{ModalCloseButton()}}, and \code{\link[=ModalBody]{ModalBody()}}) — plus other props (\code{size},
\code{centered}, \code{fullScreen}, ...). See
\url{https://mantine.dev/core/modal/#modalroot}.}

\item{inputId}{If provided, receives \code{FALSE} when the user closes it.}

\item{opened}{Initial state.}
}
\description{
The lower-level piece behind \code{\link[=Modal]{Modal()}}, for full control over the
modal's internal layout (e.g. extra controls next to the title, a
non-standard body/footer arrangement) — compose it with
\code{\link[=ModalOverlay]{ModalOverlay()}}, \code{\link[=ModalContent]{ModalContent()}}, \code{\link[=ModalHeader]{ModalHeader()}}, \code{\link[=ModalTitle]{ModalTitle()}},
\code{\link[=ModalCloseButton]{ModalCloseButton()}} and \code{\link[=ModalBody]{ModalBody()}}. Opens/closes from R exactly
like \code{\link[=Modal]{Modal()}} (\code{updateMantineProps(session, mantineId, opened = TRUE/FALSE)}), including \code{\link[=ModalStack]{ModalStack()}} participation if nested inside
one — the compound and all-in-one forms are interchangeable from R's
point of view.
}
\examples{
\dontrun{
ModalRoot("custom_modal", inputId = "custom_modal_state",
  ModalOverlay(),
  ModalContent(
    ModalHeader(ModalTitle("Custom layout"), ModalCloseButton()),
    ModalBody(Text("Modal content"))
  )
)
}
}

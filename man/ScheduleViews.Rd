% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Schedule.R
\name{DayView}
\alias{DayView}
\alias{WeekView}
\alias{MonthView}
\alias{YearView}
\title{Mantine schedule views (\verb{@mantine/schedule}): Day/Week/Month/Year}
\usage{
DayView(inputId, date = NULL, events = list(), ...)

WeekView(inputId, date = NULL, events = list(), ...)

MonthView(inputId, date = NULL, events = list(), ...)

YearView(inputId, date = NULL, events = list(), ...)
}
\arguments{
\item{inputId}{Id of the Shiny input holding the currently displayed
\code{date}.}

\item{date}{Initial displayed date (a \code{"YYYY-MM-DD"} string, or an R
\code{Date}/\code{POSIXct}).}

\item{events}{Events to display: a \code{data.frame} (or list of rows) with
\code{id}, \code{title}, \code{start}, \code{end}, and optionally \code{color}/\code{allDay}/
\code{display}/\code{recurrence} columns. \code{start}/\code{end} accept
\code{"YYYY-MM-DD HH:mm:ss"} strings or R \code{Date}/\code{POSIXct} values
(converted automatically).}

\item{...}{Other props (\code{startTime}, \code{endTime}, \code{intervalMinutes},
\code{withEventsDragAndDrop}, \code{withEventResize}, \code{withDragSlotSelect},
\code{businessHours}, \code{withCurrentTimeIndicator}, \code{firstDayOfWeek},
\code{withWeekNumbers}, ...). See \url{https://mantine.dev/schedule/day-view/}
/ \url{https://mantine.dev/schedule/week-view/} /
\url{https://mantine.dev/schedule/month-view/} /
\url{https://mantine.dev/schedule/year-view/}.}
}
\value{
A \code{mantine_element} to nest inside \code{\link[=MantineProvider]{MantineProvider()}}.
}
\description{
Calendar views for a single resource (see \code{\link[=ResourcesDayView]{ResourcesDayView()}} and
family for the resource-grouped equivalents). Fully controlled:
\code{input[[inputId]]} holds the currently displayed \code{date} (a
\code{"YYYY-MM-DD"} string), synced whenever the user navigates. Several
other interactions are additionally reported under suffixed input ids:
\itemize{
\item \code{input[[paste0(inputId, "_event_click")]]} — clicked event's \code{id}.
\item \code{input[[paste0(inputId, "_slot_click")]]} — clicked time slot (shape
depends on the view — usually a datetime string).
\item \code{input[[paste0(inputId, "_all_day_click")]]} — clicked all-day slot's
date (\code{DayView()}/\code{WeekView()}).
\item \code{input[[paste0(inputId, "_slot_select")]]} — \code{list(rangeStart=, rangeEnd=)} after a drag-to-select (needs \code{withDragSlotSelect = TRUE}), for creating a new event server-side.
\item \code{input[[paste0(inputId, "_event_drop")]]} — \code{list(eventId=, newStart=, newEnd=)} after a drag-and-drop (needs
\code{withEventsDragAndDrop = TRUE}); the view's own display already moves
the event immediately (no server round-trip needed for the visual
feedback), so this is meant for persisting the change server-side.
\item \code{input[[paste0(inputId, "_event_resize")]]} — same shape, after a
resize (needs \code{withEventResize = TRUE}).
}
}
\examples{
\dontrun{
DayView(
  inputId = "day1",
  date = Sys.Date(),
  events = data.frame(
    id = 1, title = "Team meeting",
    start = "2026-07-27 09:00:00", end = "2026-07-27 10:00:00",
    color = "blue"
  ),
  startTime = "08:00:00", endTime = "18:00:00"
)
}
}

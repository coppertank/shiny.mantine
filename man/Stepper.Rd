% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AdvancedComponents.R
\name{Stepper}
\alias{Stepper}
\alias{StepperStep}
\alias{StepperCompleted}
\title{Mantine Stepper (multi-step wizard)}
\usage{
Stepper(mantineId = NULL, inputId = NULL, active = 0, ...)

StepperStep(...)

StepperCompleted(...)
}
\arguments{
\item{mantineId}{Id used to update \code{active} reactively from R via
\code{\link[=updateMantineProps]{updateMantineProps()}}.}

\item{inputId}{If set, \code{input[[inputId]]} receives the index of the step
the user clicked.}

\item{active}{Index (0-based) of the currently active step.}

\item{...}{\code{\link[=StepperStep]{StepperStep()}}/\code{\link[=StepperCompleted]{StepperCompleted()}} children, plus other
props (\code{allowNextStepsSelect}, \code{color}, ...). See
\url{https://mantine.dev/core/stepper/}.}
}
\description{
Renders a horizontal sequence of steps. The active step is controlled by
the app (via \code{mantineId}/\code{\link[=updateMantineProps]{updateMantineProps()}}, e.g.
\code{updateMantineProps(session, "wizard", active = 1)}) — Stepper itself
does not advance automatically. If \code{inputId} is set, clicking any step
header reports its index (0-based) to \code{input[[inputId]]}, so the server
can decide whether/how to advance.
}
\examples{
\dontrun{
Stepper(
  mantineId = "wizard", inputId = "wizard_click", active = 0,
  StepperStep(label = "Step 1", description = "Create an account"),
  StepperStep(label = "Step 2", description = "Verify email"),
  StepperCompleted("All steps completed!")
)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Overlays.R
\name{ModalStack}
\alias{ModalStack}
\title{Mantine Modal.Stack (coordinated stack of Modal()s)}
\usage{
ModalStack(...)
}
\arguments{
\item{...}{\code{\link[=Modal]{Modal()}} children (each needs its own \code{mantineId}, as
usual), plus an optional \code{mantineId} of its own: if provided, lets you
close every modal in the stack at once from R with
\code{updateMantineProps(session, mantineId, closeAll = TRUE)}. See
\url{https://mantine.dev/core/modal/#stacked-modals}.}
}
\description{
A standalone \code{\link[=Modal]{Modal()}} only ever manages its own \code{opened} boolean, so
having more than one open at the same time (e.g. a confirmation modal
opening another, more specific one on top) doesn't layer/animate
correctly — each mounts its own independent overlay and focus trap,
competing rather than coordinating. Wrapping the same \code{\link[=Modal]{Modal()}}s in
\code{ModalStack()} instead delegates them to Mantine's own stack
controller, which handles z-index layering, the background-modal scale
effect, focus trapping and Escape-key handling between them.
}
\details{
Every child \code{\link[=Modal]{Modal()}} keeps working exactly as documented there —
\code{updateMantineProps(session, mantineId, opened = TRUE/FALSE)} still
opens/closes it, and \code{inputId} still receives \code{FALSE} on user close.
Being nested inside a \code{ModalStack()} is enough to switch a \code{Modal()} to
coordinated stack behavior; no other change is needed on the R side.
}
\examples{
\dontrun{
# ui:
ModalStack(
  mantineId = "delete_stack",
  Modal("delete_page", title = "Delete this page?",
    Text("This can be undone later from the trash."),
    Button("Continue", inputId = "go_to_confirm")
  ),
  Modal("confirm_delete", title = "Are you really sure?",
    Button("Yes, delete", inputId = "confirm_delete_btn", color = "red")
  )
)

# server:
observeEvent(input$go_to_confirm, {
  updateMantineProps(session, "confirm_delete", opened = TRUE)
})
observeEvent(input$confirm_delete_btn, {
  updateMantineProps(session, "delete_stack", closeAll = TRUE)
})
}
}

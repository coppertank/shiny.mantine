% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Schedule.R
\name{Schedule}
\alias{Schedule}
\title{Mantine Schedule (\verb{@mantine/schedule}): unified view with built-in switching}
\usage{
Schedule(inputId, date = NULL, view = NULL, events = list(), ...)
}
\arguments{
\item{inputId}{Id of the Shiny input holding the displayed \code{date}.}

\item{date}{Initial displayed date (a \code{"YYYY-MM-DD"} string, or an R
\code{Date}/\code{POSIXct}).}

\item{view}{Initial view level (e.g. \code{"day"}, \code{"week"}, \code{"month"},
\code{"year"}).}

\item{events}{Events to display; see \code{\link[=DayView]{DayView()}}'s \code{events}.}

\item{...}{Other props (\code{dayViewProps}, \code{weekViewProps},
\code{monthViewProps}, \code{yearViewProps} — each a named list forwarded to
the matching view — \code{withAgenda}, \code{withEventsDragAndDrop},
\code{withEventResize}, ...). See \url{https://mantine.dev/schedule/schedule/}.}
}
\value{
A \code{mantine_element} to nest inside \code{\link[=MantineProvider]{MantineProvider()}}.
}
\description{
Combines \code{\link[=DayView]{DayView()}}/\code{\link[=WeekView]{WeekView()}}/\code{\link[=MonthView]{MonthView()}}/\code{\link[=YearView]{YearView()}} behind one
component with its own header controls for switching between them.
\code{input[[inputId]]} holds the displayed \code{date};
\code{input[[paste0(inputId, "_view")]]} holds the active view level (e.g.
\code{"day"}/\code{"week"}/\code{"month"}/\code{"year"}). Other interactions are reported
the same way as \code{\link[=DayView]{DayView()}} (see \code{?ScheduleViews}).
}
\examples{
\dontrun{
Schedule(
  inputId = "sched1", view = "week",
  events = data.frame(
    id = 1, title = "Team standup",
    start = "2026-07-27 09:00:00", end = "2026-07-27 09:30:00"
  )
)
}
}

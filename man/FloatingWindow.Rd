% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AdvancedComponents.R
\name{FloatingWindow}
\alias{FloatingWindow}
\alias{FloatingWindowResizeHandle}
\title{Mantine FloatingWindow family (draggable, optionally resizable floating panel)}
\usage{
FloatingWindow(..., inputId = NULL)

FloatingWindowResizeHandle(...)
}
\arguments{
\item{...}{Props and children. See \url{https://mantine.dev/core/floating-window/}.}

\item{inputId}{Optional; if set, resize events are reported to Shiny
(see Details). Omit if you don't need to react to resizing
server-side.}
}
\description{
A draggable floating panel positioned anywhere in the viewport by the
user. \code{initialPosition} sets where it starts (plain \code{list(top=, left=)}/\code{list(bottom=, right=)} data) — the window's \emph{live} position
while being dragged isn't readable/settable from the server, since that
needs a client-side ref. Its parent needs \code{position: relative} (or
similar) for it to be positioned against it.
}
\details{
Add \code{dimensions} (\code{list(initialWidth=, minWidth=, maxWidth=, initialHeight=, minHeight=, maxHeight=)}) together with a
\code{\link[=FloatingWindowResizeHandle]{FloatingWindowResizeHandle()}} child to make it resizable (drag or,
when the handle has keyboard focus, Arrow keys/Home/End). If \code{inputId}
is set, resizing (drag only — not keyboard) is reported to Shiny:
\code{input[[inputId]]} holds the current \code{list(width=, height=)} after
every resize, and \code{input[[paste0(inputId, "_resize_start")]]}/
\code{input[[paste0(inputId, "_resize_end")]]} fire as events when a
drag-resize begins/ends (added in Mantine 9.5.1).
}
\examples{
\dontrun{
Box(
  style = list(position = "relative", height = "300px"),
  FloatingWindow(
    inputId = "win_size",
    dimensions = list(initialWidth = 220, minWidth = 150, maxWidth = 400,
                       initialHeight = 140, minHeight = 100, maxHeight = 300),
    style = list(border = "1px solid #ddd", background = "white", padding = "12px"),
    Text("Drag / resize me."),
    FloatingWindowResizeHandle(style = list(position = "absolute", right = 0, bottom = 0))
  )
)
}
}

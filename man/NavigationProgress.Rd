% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NProgress.R
\name{NavigationProgress}
\alias{NavigationProgress}
\alias{startMantineProgress}
\alias{setMantineProgress}
\alias{incrementMantineProgress}
\alias{decrementMantineProgress}
\alias{completeMantineProgress}
\alias{resetMantineProgress}
\title{Mantine NavigationProgress (progress bar at the top of the page)}
\usage{
NavigationProgress(...)

startMantineProgress(session = shiny::getDefaultReactiveDomain())

setMantineProgress(value, session = shiny::getDefaultReactiveDomain())

incrementMantineProgress(
  value = NULL,
  session = shiny::getDefaultReactiveDomain()
)

decrementMantineProgress(
  value = NULL,
  session = shiny::getDefaultReactiveDomain()
)

completeMantineProgress(session = shiny::getDefaultReactiveDomain())

resetMantineProgress(session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{...}{Optional props (\code{color}, \code{size}, \code{zIndex}, ...). See
\url{https://mantine.dev/x/nprogress/}.}

\item{session}{Shiny session.}

\item{value}{Percentage value (0-100).}
}
\description{
Should be inserted once in the UI (typically inside \code{MantineProvider()},
the same way as \code{Notifications()}); it is then driven from the server
with the imperative functions \code{startMantineProgress()},
\code{setMantineProgress()}, \code{incrementMantineProgress()},
\code{decrementMantineProgress()}, \code{completeMantineProgress()}, and
\code{resetMantineProgress()}.
}
\examples{
\dontrun{
# ui:
MantineProvider(NavigationProgress(), ...)

# server:
observeEvent(input$go, {
  startMantineProgress(session)
  ...
  completeMantineProgress(session)
})
}
}
